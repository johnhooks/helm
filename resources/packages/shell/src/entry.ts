/**
 * Helm Shell webpack entry point.
 *
 * Composed components and hooks that bridge `@helm/ui` with `@helm/core`.
 * Built as a standalone WordPress script (handle: helm-shell).
 */
export * from './index';
