/**
 * Bridge — the main command view.
 *
 * Ship status, commands, and overview. Renders inside normal WP-Admin chrome.
 */
export function BridgePage() {
	return (
		<div>
			<h2>Bridge</h2>
			<p>Ship command view placeholder.</p>
		</div>
	);
}
