{
	"name": "@helm/core",
	"version": "0.1.0",
	"description": "Shared Helm runtime — bundles data, errors, and logger into a single WordPress script.",
	"private": true,
	"main": "src/index.ts",
	"types": "src/index.ts",
	"dependencies": {
		"@helm/data": "workspace:*",
		"@helm/errors": "workspace:*",
		"@helm/logger": "workspace:*",
		"@helm/ui": "workspace:*"
	},
	"peerDependencies": {
		"react": "^18.3.1"
	}
}
